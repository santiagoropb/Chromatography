language: python
python:
- "3.5"
matrix:
  include:
    - os: linux
      sudo: False
install:
- pip install -r requirements.txt
script:
- nosetests pycadet/tests/ -v --with-coverage --cover-package=pycadet